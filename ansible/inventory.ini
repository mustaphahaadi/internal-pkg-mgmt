# -------------------------------------------------------
# Ansible Inventory
# Lists all client containers Ansible will manage
# Docker bridge network resolves hostnames automatically
# -------------------------------------------------------

[clients]
web-server
db-server
backup-server

[clients:vars]
ansible_user=root
ansible_password=ansible
ansible_connection=ssh
