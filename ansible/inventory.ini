# -------------------------------------------------------
# Ansible Inventory
# Lists all client containers Ansible will manage
# Docker bridge network resolves hostnames automatically
# -------------------------------------------------------

[clients]
web-server
db-server
backup-server

[clients:vars]
# Use local connection and call Docker directly
ansible_connection=local
